{
  "name": "auth-example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "zig build",
    "dev": "npx wrangler@latest dev --port 8787",
    "deploy": "npx wrangler@latest deploy",
    "db:create": "npx wrangler d1 create auth-db",
    "db:migrate": "npx wrangler d1 execute auth-db --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3"
  }
}
